/**
 * Prisma lib: owns the schema, migrations, and the generated (checked-in)
 * Prisma 7 TypeScript client. Everything Prisma-related is imported from
 * `@smart-home-inventory/prisma` — never from `@prisma/client` directly.
 */
export * from './generated/client';
