/** Default marker (dot) color — used whenever a Marker has no custom `color`. */
export const DEFAULT_NODE_COLOR = '#4fc3f7';

/** AV-rack-inspired dark palette for box/box-ports/image — matches the
 *  sibling ng-diagram-av-schematic reference project's device-node styling
 *  (gray card border, near-black fill/header, gray port-row background)
 *  rather than the app's light/dark theme, which read too colorful (blue)
 *  by default. */
export const DEFAULT_BORDER_COLOR = '#383a40';
export const DEFAULT_BODY_COLOR = '#151516';
export const DEFAULT_PORTS_BG_COLOR = '#27282b';
export const DEFAULT_SEPARATOR_COLOR = '#4d5059';
